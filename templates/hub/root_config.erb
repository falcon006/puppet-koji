### THIS FILE IS MANAGED BY PUPPET
### <%= file.split(File::SEPARATOR)[5..-1].join(File::SEPARATOR) %>

[koji]

;configuration for koji cli tool

;url of XMLRPC server
server = http://<%= @fqdn %>/kojihub

;url of web interface
weburl = http://<%= @fqdn %>/koji

;topurl
topurl = http://<%= @fqdn %>/kojifiles

;path to the koji top directory
topdir = /mnt/koji

;configuration for SSL athentication

;client certificate
cert = /etc/pki/koji/certs/kojiadmin.pem

;certificate of the CA that issued the client certificate
ca = /etc/pki/koji/certs/koji_ca_cert.crt

;certificate of the CA that issued the HTTP server certificate
serverca = /etc/pki/koji/certs/koji_ca_cert.crt
